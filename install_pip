sudo python3 -m pip install --upgrade \
fabric `# tool for deployment` \
pexpect `# remote-control interactive bash processes from python` \
neovim `# neovim plugin` \
grip `# github flavored markdown preview` \
awscli `# amazon webservices command line interface` \
docker-compose `# docker stuff` \
powerline-status \
httpie `# like curl but with json support` \
flake8 `# pep8 and complexity` \
autopep8 `# autocorrect pep8 errors` \
invoke `# the non-ssh part of fabric 2` \
ipython `# interactive python` \
psutil `# for tmux-powerline` \
pyyaml `# yaml` \
pynvim `# neovim plugin` \
pipenv `# better package manager?` \
pymode `# helper for pymode plugin in vim` \
tmuxp `# tmux setup` \
python-language-server `# pyls` \
git-devbliss `# git-devbliss from github` \

[core]
	quotepath = false
[color]
	diff = auto
	status = auto
    branch = auto
[merge]
	conflictstyle = diff3
[diff]
    mnemonicprefix = true
[user]
	name = Wolfgang Pfnür
	email = wolfgang.pfnuer@swaarm.com
[push]
  default = current
  followTags = true
[pull]
    default = current
	rebase = false
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	required = true
	process = git-lfs filter-process
